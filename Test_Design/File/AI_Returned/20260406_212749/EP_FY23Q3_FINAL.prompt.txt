[SYSTEM PROMPT]
You are a document extraction model. Read the attached PDF carefully and return only JSON. Do not include markdown fences, explanations, or any text outside JSON.

[USER PROMPT]
Read the attached PDF and produce output that is directly useful for downstream chunking and embedding.

Output requirements:
1. Return ONLY one JSON object.
2. The top-level object must use this minimum wrapper:
{
  "source_file": "Test_Design/File/Flex/Earnings Presentation/EP_FY23Q3_FINAL.pdf",
  "output_mode": "chunks | chunk_ready | parsed",
  "content": ...
}
3. content can use whatever internal structure is best for this PDF. Do not force a generic schema if the document suggests a better structure.

Priority:
- First choice: high-quality chunk-ready output or a chunk list.
- Second choice: chunk-ready structured content.
- Third choice: the cleanest parsed result for later chunking.

Quality requirements:
- semantic accuracy
- strong coverage of important content
- preserve heading hierarchy when possible
- keep paragraph boundaries natural
- keep bullets readable
- keep tables readable, either as compact row objects or clear text blocks
- remove page headers, page footers, repeated slide headers, repeated legal boilerplate, page numbers, and obvious noise
- each text block should be as complete a semantic unit as possible

If you output chunks, prefer meaningful chunks over tiny fragments. Include lightweight metadata only when it helps chunking, such as section title, page number, page range, or chunk label.
