# ============================================
# CapEx Intelligence Platform - Environment Variables
# ============================================
# Copy this file to .env and fill in your values

# LLM Provider: "openai", "anthropic", or "gemini"  (default: openai)
LLM_PROVIDER=openai

# OpenAI API  (required when LLM_PROVIDER=openai)
# Get key at: https://platform.openai.com/api-keys
# Uses dual-model strategy:
#   - gpt-4o for response generation (stronger)
#   - gpt-4o-mini for reranking (cheaper, faster)
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic API  (required when LLM_PROVIDER=anthropic)
# Get key at: https://console.anthropic.com/
# Uses: claude-sonnet-4-6 (main) + claude-haiku-4-5 (fast)
ANTHROPIC_API_KEY=

# Gemini API  (required when LLM_PROVIDER=gemini)
# Get key at: https://aistudio.google.com/app/apikey
# Used by HyperScaler article extraction / later Gemini-backed refresh flows
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3-flash

# Web Search - Brave API (Optional - FREE tier: 2,000 queries/month)
# Get key at: https://brave.com/search/api/
BRAVE_API_KEY=BSA-your-key-here

# Email Alerts - SendGrid (Optional - FREE tier: 100 emails/day)
# Get key at: https://sendgrid.com/
SENDGRID_API_KEY=SG.your-key-here
ALERT_FROM_EMAIL=alerts@yourdomain.com

# Slack Integration (Optional)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_SIGNING_SECRET=your-signing-secret
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz

# Google Calendar (Optional)
GOOGLE_CALENDAR_CREDENTIALS=./credentials.json
GOOGLE_CALENDAR_ID=primary

# SEC Scraper
SEC_USER_AGENT=CapExIntel/1.0 (your-email@example.com)

# Scheduler (Cron format: 4 PM ET on weekdays)
INGESTION_SCHEDULE=0 16 * * 1-5
